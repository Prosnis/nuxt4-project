# Nuxt 4 Project

Полнофункциональный проект на Nuxt 4 с современным стеком технологий.

## 🚀 Технологии

- **Nuxt 4** - фреймворк для Vue.js
- **TypeScript** - типизация
- **Composition API** - современный API Vue
- **Pinia** - управление состоянием
- **i18n** - интернационализация
- **SASS** - препроцессор CSS
- **ESLint** - линтер кода

## 📁 Структура проекта

Проект использует новую структуру директорий Nuxt 4 с папкой `app/`:

```
app/
├── assets/         # Статические ресурсы (стили, изображения)
├── components/     # Vue компоненты
├── composables/    # Composable функции
├── layouts/        # Layouts приложения
├── middleware/     # Middleware для роутинга
├── pages/          # Страницы приложения
├── plugins/        # Плагины Nuxt
├── stores/         # Pinia stores
└── utils/          # Утилиты

locales/           # Файлы переводов для i18n
public/            # Публичные файлы
server/            # Серверная часть
```

## 🛠️ Установка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview

# Линтинг кода
npm run lint

# Автоматическое исправление ошибок линтера
npm run lint:fix
```

## 🌐 i18n

Приложение поддерживает мультиязычность:
- Английский (en) - язык по умолчанию
- Русский (ru)

Переводы находятся в папке `locales/`.

## 📦 Основные возможности

### Pinia Store
Используйте Composition API стиль для создания stores:
```typescript
import { defineStore } from 'pinia'

export const useMyStore = defineStore('myStore', () => {
  // state
  const count = ref(0)
  
  // actions
  function increment() {
    count.value++
  }
  
  return { count, increment }
})
```

### Composables
Все файлы в `app/composables/` автоматически импортируются.

### Auto-imports
Nuxt 4 автоматически импортирует:
- Vue APIs (ref, computed, etc.)
- Nuxt APIs (navigateTo, useRoute, etc.)
- Компоненты из `app/components/`
- Composables из `app/composables/`

## 📝 Лицензия

MIT